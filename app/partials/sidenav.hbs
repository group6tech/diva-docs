{{#each categories }}
	<h3>{{ category }}</h3>
	<ul class="nav">
		{{#each pages}}
			<li><a href="/docs/{{../../version}}/{{data.categories}}/{{filename}}">{{ data.title }}</a></li>
		{{/ each}}
	</ul>
{{/each }}
